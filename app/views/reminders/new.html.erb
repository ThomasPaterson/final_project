<div class="reminderpages">	
<div class="content">
<h1>Create a reminder for 
	<% if @user == current_user %>
		yourself
	<% else %>
		<%= @user.name %>
	<% end %>
</h1>

<%= render 'form' %>


</div>
</div>